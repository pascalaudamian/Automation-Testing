// filepath: src/app/api/auth/user/route.ts
try {
  // Add the code that might throw an error here
} catch (error: unknown) { // Changed 'any' to 'unknown'
  console.error('Error occurred during signup:', error);
}

// filepath: src/app/date-time-pickers/page.tsx
const formatted = ""; // Should be const